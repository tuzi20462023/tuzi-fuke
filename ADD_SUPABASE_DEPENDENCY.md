# 🔧 添加Supabase依赖 - 详细步骤指南

**当前错误**: `No such module 'Supabase'`
**原因**: 项目中还没有添加Supabase iOS SDK依赖
**解决方案**: 使用Swift Package Manager添加依赖

---

## 📱 操作步骤 (在Xcode中执行)

### 第1步: 打开项目

```bash
cd /Users/mikeliu/Desktop/tuzi-fuke
open tuzi-fuke.xcodeproj
```

### 第2步: 添加Package依赖

**详细点击步骤:**

1. **选择项目文件**
   
   - 在Xcode左侧导航器中，点击最顶层的 `tuzi-fuke` (蓝色项目图标)

2. **进入Package Dependencies**
   
   - 在中间面板选择 `tuzi-fuke` target
   - 点击顶部的 `Package Dependencies` 标签页

3. **添加新依赖**
   
   - 点击左下角的 `+` 按钮
   - 在弹出的对话框中输入URL:
     
     ```
     https://github.com/supabase/supabase-swift
     ```
   - 点击 `Add Package`

4. **等待依赖解析**
   
   - Xcode会自动下载和解析依赖包
   - 这个过程可能需要1-2分钟

5. **选择要添加的产品**
   
   - 在弹出的产品列表中，勾选以下选项:
     - ✅ **Supabase** (核心库，必需)
     - ✅ **Auth** (认证功能)
     - ✅ **PostgREST** (数据库操作)
     - ⚪ Realtime (实时功能，可选)
     - ⚪ Storage (文件存储，可选)

6. **确认添加**
   
   - 点击 `Add Package` 完成添加

### 第3步: 验证依赖添加成功

**检查Package Dependencies:**

- 在左侧导航器中应该能看到 `Package Dependencies` 节点
- 展开后应该显示 `supabase-swift` 包

---

## 🔧 添加完成后的修复步骤

### 1. 恢复SupabaseConfig.swift

添加依赖成功后，需要恢复SupabaseConfig.swift中的Supabase导入:

**修改这一行:**

```swift
// 🚨 临时注释: import Supabase (等待SPM依赖添加完成)
```

**改为:**

```swift
import Supabase
```

**并添加回SupabaseClient:**

```swift
/// 共享的Supabase客户端实例
static let shared: SupabaseClient = {
    return SupabaseClient(
        supabaseURL: supabaseURL,
        supabaseKey: supabaseAnonKey
    )
}()
```

### 2. 编译测试

**运行编译:**

```bash
⌘+B (Command + B)
```

**预期结果:**

- ✅ 编译成功，无错误
- ✅ `import Supabase` 不再报错
- ✅ SupabaseClient可正常使用

---

## 🚨 常见问题解决

### 问题1: "Failed to resolve package"

**解决方案:**

1. 检查网络连接
2. 尝试使用SSH URL: `git@github.com:supabase/supabase-swift.git`
3. 清理Xcode缓存: Product → Clean Build Folder

### 问题2: "Package is not compatible"

**解决方案:**

1. 确认项目的iOS目标版本是15.0+
2. 确认Swift版本是5.0+

### 问题3: 添加后仍然报错

**解决方案:**

1. 清理构建: ⌘+Shift+K
2. 重新构建: ⌘+B
3. 重启Xcode

---

## 📋 成功验证清单

添加依赖完成后，检查以下项目:

- [ ] Package Dependencies中显示supabase-swift
- [ ] 编译项目成功 (⌘+B)
- [ ] `import Supabase` 不报错
- [ ] 运行应用显示配置信息

---

## 🎯 完成后的下一步

依赖添加成功后，你需要:

1. **恢复完整的SupabaseConfig.swift**
2. **注册Supabase账号并创建项目**
3. **更新配置中的URL和密钥**
4. **开始Day 1下午的Manager开发**

---

## ⚡ 快速验证命令

```bash
# 编译检查
⌘+B

# 运行检查
⌘+R

# 预期控制台输出:
🚀 [App] 地球新主启动中...
🔧 Supabase 配置信息
SDK状态: ✅ 依赖已添加
```

**现在请按照以上步骤在Xcode中添加Supabase依赖！** 🚀